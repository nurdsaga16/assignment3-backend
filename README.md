# AzharFund - Благотворительная платформа

Полнофункциональная платформа для благотворительности, соединяющая доноров и нуждающихся в помощи. Проект реализован на Spring Boot с использованием PostgreSQL и JWT аутентификации.

## 📋 Описание

AzharFund - это веб-платформа, которая позволяет:
- **Донорам** делать пожертвования на различные проекты
- **Нуждающимся** создавать проекты для сбора средств после верификации
- **Администраторам** управлять пользователями, проектами и категориями
- **Волонтерам** присоединяться к волонтерским событиям

## 🚀 Технологии

- **Backend Framework**: Spring Boot 3.x
- **Database**: PostgreSQL
- **Security**: Spring Security с JWT
- **ORM**: Spring Data JPA / Hibernate
- **Build Tool**: Maven
- **Java Version**: 17+
- **Mapping**: MapStruct

## 📦 Требования

- Java 17 или выше
- Maven 3.6+
- PostgreSQL 12+
- Git

## 🔧 Установка и запуск

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd project
```

### 2. Настройка базы данных

Создайте базу данных PostgreSQL:

```sql
CREATE DATABASE assignment3;
```

### 3. Настройка переменных окружения

Создайте файл `.env` или установите переменные окружения:

```bash
export JWT_SECRET_KEY=your-secret-key-here
export DB_PASSWORD=your-database-password
```

Или отредактируйте `src/main/resources/application.properties`:

```properties
spring.datasource.url=jdbc:postgresql://localhost:5432/assignment3
spring.datasource.username=postgres
spring.datasource.password=your-password

security.jwt.secret-key=${JWT_SECRET_KEY:your-default-secret-key}
```

### 4. Запуск приложения

```bash
mvn clean install
mvn spring-boot:run
```

Приложение будет доступно по адресу: `http://localhost:8080`

## 📚 API Документация

### Базовый URL
```
http://localhost:8080/api/v1
```

### Аутентификация

#### Регистрация
```http
POST /auth/register
Content-Type: application/json

{
  "fullName": "Иван Иванов",
  "email": "user@example.com",
  "password": "password123",
  "role": "DONOR" | "NEEDS_HELP" | "ADMIN"
}
```

#### Вход
```http
POST /auth/login
Content-Type: application/json

{
  "email": "user@example.com",
  "password": "password123"
}
```

**Ответ:**
```json
{
  "token": "jwt-token-here",
  "email": "user@example.com"
}
```

### Проекты

#### Получить все проекты (публичный)
```http
GET /projects
```

#### Получить проект по ID (публичный)
```http
GET /projects/{id}
```

#### Создать проект (требует аутентификации, только NEEDS_HELP с isVerified=true или ADMIN)
```http
POST /projects
Authorization: Bearer {token}
Content-Type: application/json

{
  "title": "Название проекта",
  "description": "Описание проекта",
  "categoryId": 1,
  "targetAmount": 100000,
  "collectedAmount": 0,
  "authorId": 1,
  "imageIds": [1, 2, 3]
}
```

#### Обновить проект (только автор или ADMIN)
```http
PUT /projects/{id}
Authorization: Bearer {token}
Content-Type: application/json

{
  "title": "Обновленное название",
  "description": "Обновленное описание",
  "categoryId": 1,
  "targetAmount": 150000,
  "authorId": 1,
  "imageIds": [1, 2]
}
```

#### Удалить проект (только автор или ADMIN)
```http
DELETE /projects/{id}
Authorization: Bearer {token}
```

#### Верифицировать проект (только ADMIN)
```http
PUT /projects/{id}/verify
Authorization: Bearer {token}
```

### Пожертвования

#### Создать пожертвование (требует аутентификации, только DONOR)
```http
POST /donations
Authorization: Bearer {token}
Content-Type: application/json

{
  "amount": 5000,
  "projectId": 1
}
```

#### Получить историю пожертвований проекта (публичный)
```http
GET /projects/{id}/donations
```

#### Статистика пожертвований (публичный)
```http
GET /donations/count          # Общее количество
GET /donations/month-sum      # Сумма за текущий месяц
GET /donations/last-year-sum  # Сумма за прошлый год
```

### Пользователи

#### Получить всех пользователей (публичный)
```http
GET /users
```

#### Получить текущего пользователя
```http
GET /users/me
Authorization: Bearer {token}
```

#### Создать пользователя
```http
POST /users
Content-Type: multipart/form-data

fullName: Иван Иванов
email: user@example.com
password: password123
role: DONOR
avatar: (file, optional)
document: (file, optional, только для NEEDS_HELP)
```

#### Обновить пользователя
```http
PUT /users/{id}
Authorization: Bearer {token}
Content-Type: multipart/form-data
```

#### Верифицировать пользователя (только ADMIN)
```http
PUT /users/{id}/verify
Authorization: Bearer {token}
```

### Категории

#### Получить все категории (публичный)
```http
GET /categories
```

#### Создать категорию (только ADMIN)
```http
POST /categories
Authorization: Bearer {token}
Content-Type: application/json

{
  "name": "Помощь больным",
  "description": "Описание категории"
}
```

#### Обновить категорию (только ADMIN)
```http
PUT /categories/{id}
Authorization: Bearer {token}
```

#### Удалить категорию (только ADMIN)
```http
DELETE /categories/{id}
Authorization: Bearer {token}
```

### Волонтерские события

#### Получить все события (публичный)
```http
GET /events
```

#### Создать событие (только ADMIN)
```http
POST /events
Authorization: Bearer {token}
Content-Type: application/json

{
  "title": "Название события",
  "description": "Описание",
  "eventDate": "2025-12-25T10:00:00",
  "location": "Алматы, ул. Абая 150",
  "organizerId": 1
}
```

#### Присоединиться к событию
```http
POST /events/{id}/join
Authorization: Bearer {token}
```

#### Покинуть событие
```http
POST /events/{id}/leave
Authorization: Bearer {token}
```

## 🔐 Роли пользователей

- **DONOR** - Донор (автоматически верифицирован при регистрации)
- **NEEDS_HELP** - Нуждающийся (требует верификации для создания проектов)
- **ADMIN** - Администратор (полный доступ ко всем функциям)

## 🔒 Безопасность

- JWT токены для аутентификации
- Spring Security для авторизации
- Валидация входных данных (Jakarta Validation)
- Проверка размера файлов (максимум 10MB)
- Проверка типов файлов (изображения: JPG, PNG, WEBP, GIF; документы: PDF)
- Ограничения на длину строковых полей
- Секретный ключ JWT через переменные окружения

## 📁 Структура проекта

```
project/
├── src/
│   ├── main/
│   │   ├── java/com/assignment3/project/
│   │   │   ├── config/          # Конфигурация
│   │   │   ├── controllers/     # REST контроллеры
│   │   │   ├── dto/             # Data Transfer Objects
│   │   │   ├── entities/        # JPA сущности
│   │   │   ├── enums/           # Перечисления
│   │   │   ├── exceptions/      # Обработка ошибок
│   │   │   ├── mappers/         # MapStruct мапперы
│   │   │   ├── repositories/    # JPA репозитории
│   │   │   ├── security/        # Spring Security
│   │   │   └── services/        # Бизнес-логика
│   │   └── resources/
│   │       └── application.properties
│   └── test/
└── pom.xml
```

## 🗄️ База данных

### Основные сущности:
- **User** - Пользователи (доноры, нуждающиеся, администраторы)
- **Project** - Проекты для сбора средств
- **Category** - Категории проектов
- **Donation** - Пожертвования
- **VolunteerEvent** - Волонтерские события
- **Image** - Изображения проектов

## 🚀 Автоматическая инициализация

При первом запуске приложения автоматически создается администратор:
- Email: `admin@azharfund.kz`
- Password: `admin123`
- Role: `ADMIN`

**⚠️ ВАЖНО**: Измените пароль администратора в продакшене!

## 📝 Особенности

- Автоматическая верификация доноров при регистрации
- Верификация нуждающихся администратором
- Верификация проектов администратором
- Отображение только верифицированных проектов в публичных списках
- История пожертвований для каждого проекта
- Статистика пожертвований
- Управление волонтерскими событиями
- Загрузка изображений и документов

## 🧪 Тестирование

```bash
mvn test
```

## 📄 Лицензия

Этот проект создан в образовательных целях.

## 👥 Авторы

- Nurdaulet Sagnadin

## 📞 Контакты

Для вопросов и предложений создайте Issue в репозитории.

